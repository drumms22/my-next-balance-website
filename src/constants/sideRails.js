/**
 * Slide-out overlay rails (recurring expenses + financial setup).
 *
 * These rails overlay the main content (we do NOT shift the center column),
 * so they can be wide without squishing the projection UI.
 */
export const RAIL_OVERLAY_WIDTH_PX = "min(90vw, 1300px)";

/** Horizontal padding for the main content column. */
export const MAIN_CONTENT_PADDING_X_PX = 20;

export const RAIL_SLIDE_TRANSITION = "transform 0.32s cubic-bezier(0.4, 0, 0.2, 1)";
